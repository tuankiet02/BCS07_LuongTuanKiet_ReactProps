import React from "react";
import ReactDOM from "react-dom/client";

import RenderShoes from "./RenderShoe/RenderShoes";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <>
    <RenderShoes />
  </>
);
